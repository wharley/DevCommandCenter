# v0.1.79 Release Notes

## Provider catalog

- Update the Claude catalog to include Opus 5.5.
- Keep provider selection, delegation instructions, and model registry tests in
  sync with the new model entry.

## Validation

- Targeted provider model registry tests passed.
- Sidecar tests passed.
- `git diff --check` passed.
- The OJ cache will be warmed from `main` before the release tag is published,
  including the macOS Intel runner.
