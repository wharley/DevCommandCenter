# v0.1.83 Release Notes

## Browser

- Approve browser requests from a floating macOS panel while keeping the main
  Dev Command Center window in the background.
- Keep approved Browser work running when its parent window is hidden.
- Refresh and validate requests before applying approval from the floating
  panel.

## Validation

- Browser approval host and occlusion tests were added.
- `git diff --check` passed.
