# v0.1.55 Release Notes

## Sidebar footer

- Keep Usage, Skills, Settings, and Update simultaneously visible in the
  expanded sidebar without adding horizontal scrolling or increasing its width.
- Present the footer destinations as compact fixed-size icon buttons with
  accessible labels and tooltips while preserving the Skills count badge.
- Keep the available update action in its existing position and move its
  version details into a tooltip so the button no longer expands or gets
  clipped on hover.

## Validation before publishing

- Pass the complete desktop test suite with 473 tests, desktop type checks, and
  the production frontend build.
- Confirm the release diff has no whitespace errors.
- Confirm release metadata is `0.1.55` in the root package, sidecar, Cargo, lock,
  and Tauri configuration files.
- Let the release workflow build signed macOS and Linux artifacts and create the
  GitHub draft release for final publication.
