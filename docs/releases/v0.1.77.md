# v0.1.77 Release Notes

## Critical workspace delivery fix

- Respect the selected **Local direto** execution mode during commit and push.
- Keep local-direct work on the currently checked-out branch instead of silently
  creating a `dcc/...` branch.
- Restrict automatic branch creation to protected worktrees.
- Resolve the active workspace correctly when local and protected tasks share a
  repository root.

## Validation

- Targeted Tauri push and workspace tests passed.
- `git diff --check` passed.
- GitHub Actions are enabled and the release workflow is active.
