# v0.1.82 Release Notes

## Quick Composer

- Search project files with `@` before creating a task; references use paths
  relative to the selected project in Local and Worktree modes.
- Keep file suggestions visible inside the floating composer and show previews
  from the selected project.
- Refresh the previous-task notice when a task is completed, archived, or removed.
- Refine the model picker for the compact window with search, favorites, effort,
  and response-style controls in one panel.

## Validation

- Quick Composer delivery, file mention, and workspace composer tests passed.
- TypeScript and the desktop build passed.
- `git diff --check` passed.
