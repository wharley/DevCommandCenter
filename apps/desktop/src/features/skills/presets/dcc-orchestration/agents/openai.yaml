interface:
  display_name: "DCC Orchestration"
  short_description: "Coordinate complex work with native subagents"
  default_prompt: "Use $dcc-orchestration to coordinate this task with suitable subagents."

policy:
  allow_implicit_invocation: true
