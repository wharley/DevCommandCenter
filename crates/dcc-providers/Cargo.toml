[package]
name = "dcc-providers"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
async-trait = "0.1"
dcc-core = { path = "../dcc-core" }
futures = "0.3"
tokio = { version = "1", features = ["process", "rt", "macros"] }
uuid = { version = "1", features = ["v4"] }
